services:
  test:
    build:
      context: .
      target: test
    environment:
      DATABASE_URL: "sqlite:///:memory:"
      PYTHONPATH: "/app"
    command: pytest --cov=app --cov-report=term-missing -v
