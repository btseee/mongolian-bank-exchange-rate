services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mongolian-bank-api
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-bankuser}:${POSTGRES_PASSWORD:-bankpass123}@db:5432/${POSTGRES_DB:-exchange_rates}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      TRUST_PROXY_HEADERS: ${TRUST_PROXY_HEADERS:-false}
      RATE_LIMIT_ENABLED: ${RATE_LIMIT_ENABLED:-true}
      RATE_LIMIT_REQUESTS: ${RATE_LIMIT_REQUESTS:-60}
      RATE_LIMIT_WINDOW_SECONDS: ${RATE_LIMIT_WINDOW_SECONDS:-60}
      RATE_LIMIT_MAX_CLIENTS: ${RATE_LIMIT_MAX_CLIENTS:-10000}
      API_MAX_LIMIT: ${API_MAX_LIMIT:-100}
      ADMIN_API_KEY: ${ADMIN_API_KEY:-}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - bank-network
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import requests; requests.get('http://localhost:8000/api/health', timeout=5)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  cron:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mongolian-bank-cron
    command: python -m scripts.cron
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-bankuser}:${POSTGRES_PASSWORD:-bankpass123}@db:5432/${POSTGRES_DB:-exchange_rates}
      CRON_SCHEDULE: ${CRON_SCHEDULE:-0 9 * * *}
      ENABLE_PARALLEL: ${ENABLE_PARALLEL:-true}
      MAX_WORKERS: ${MAX_WORKERS:-8}
      PLAYWRIGHT_MAX_WORKERS: ${PLAYWRIGHT_MAX_WORKERS:-3}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - bank-network

  db:
    image: postgres:15-alpine
    container_name: mongolian-bank-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-bankuser}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-bankpass123}
      - POSTGRES_DB=${POSTGRES_DB:-exchange_rates}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    networks:
      - bank-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bankuser}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  bank-network:
    driver: bridge
